# Diabetes Consultant (UK-based) — fixed requirements for Streamlit Cloud (Python 3.13)
# Pinned to versions with manylinux wheels; lxml upgraded to avoid source build.

streamlit==1.48.1
pydantic==2.11.7
openai==1.101.0
python-dotenv==1.1.1

# Settings
pydantic-settings==2.10.1

# Vector + numerics
faiss-cpu==1.12.0
numpy==2.3.2
pandas==2.3.2
python-dateutil==2.9.0.post0
packaging==23.2

# NLP / embeddings / DB
sentence-transformers==2.7.0
chromadb==0.4.24
rank-bm25==0.2.2

# PDF / charts / images
reportlab==4.4.3
plotly==5.24.1
Pillow==10.4.0

# Streamlit add-ons
streamlit-extras==0.3.6
st-pages==0.4.5
streamlit-option-menu==0.3.13
streamlit-tags==1.2.8

# Web utils
beautifulsoup4==4.13.4
requests==2.32.5

# FIX: lxml build error on Py 3.13 -> use binary wheel from 5.x
lxml>=5.2.1,<6
